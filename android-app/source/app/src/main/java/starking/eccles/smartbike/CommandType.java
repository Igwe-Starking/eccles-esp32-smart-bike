package starking.eccles.smartbike;

/** Mirrors Eccles::CommandType (Engine.h). Only TEXT_BINARY exists today. */
public final class CommandType {
    private CommandType() {}
    public static final int TEXT_BINARY = 0;
}
